import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    item: {
        padding: 10,
        marginVertical: 8,
        marginHorizontal: 16,
    },
    name: {
        color: 'black',
        fontSize: 20,
        fontWeight: 'bold',
    },
    role: {
        color: 'black',
        fontSize: 16,
    },
});
