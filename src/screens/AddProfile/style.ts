import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonDropDown: {
        borderRadius: 10,
        alignSelf: 'center',
        marginTop: 10,
        width: '60%',
    },
    dropDown: {
        borderRadius: 10,
    },
    phoneNumberInput: {
        borderRadius: 10,
        borderWidth: 2,
        borderColor: 'black',
        color: 'black',
        width: '70%',
        justifyContent: 'center',
        alignContent: 'center',
        textAlign: 'center',
        fontSize: 20,
    },
});
