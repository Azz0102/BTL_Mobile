import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonDropDown: {
        borderRadius: 10,
        alignSelf: 'center',
        marginTop: 10,
        width: '60%',
    },
    dropDown: {
        borderRadius: 10,
    },
});
